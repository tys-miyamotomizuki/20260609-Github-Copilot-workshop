# ポモドーロタイマーアプリ 実装機能一覧

## 1. 概要

このドキュメントは、ポモドーロタイマー Web アプリケーションを実装するにあたって必要となる機能を整理したものです。

機能は以下の 3 つの観点で分類します。

- MVP として最初に実装すべき機能
- 使い勝手や保守性のために早めに入れたい補助機能
- 将来拡張として段階的に追加できる機能


## 2. MVP として実装する機能

### 2.1 画面表示

- Flask でトップ画面を返す
- 単一画面のポモドーロタイマー UI を表示する
- 添付モックに近いレイアウトを再現する
- デスクトップ幅で自然に見えるタイマー表示、操作ボタン、進捗カードを配置する


### 2.2 タイマー基本操作

- 作業セッションを開始できる
- 進行中のタイマーを一時停止できる
- 一時停止したタイマーを再開できる
- タイマーを初期状態にリセットできる
- 残り時間を分:秒形式で表示できる


### 2.3 タイマー状態管理

- 作業中、短い休憩、長い休憩などのモードを扱える
- idle、running、paused などの状態を管理できる
- セッション終了時に完了判定を行える
- 現在モードを画面上に表示できる


### 2.4 進捗表示

- 円形プログレスリングで進捗率を表示できる
- 残り時間に応じてリング表示を更新できる
- タイマー状態に応じて開始、一時停止、再開、リセットの UI を切り替えられる


### 2.5 当日実績表示

- 今日完了したポモドーロ回数を表示できる
- 今日の集中時間合計を表示できる
- 作業セッション完了時に回数と集中時間を更新できる


## 3. MVP で実装したい補助機能

### 3.1 状態復元

- リロード後にタイマー状態を復元できる
- リロード後に当日実績を復元できる
- 保存データが不正な場合でも安全に初期状態へ戻せる


### 3.2 時間ずれ対策

- 終了予定時刻ベースで残り時間を再計算できる
- タブ非アクティブ時でも時間ずれを最小限に抑えられる
- 再描画遅延があっても正しい完了判定ができる


### 3.3 日付切り替え対応

- 日付変更時に当日実績をリセットできる
- 前日の実績と当日の実績を混在させない


### 3.4 基本的なユーザビリティ

- ボタンの状態を現在のタイマー状態に応じて制御できる
- 初期状態、実行中、一時停止中の違いが UI 上でわかる
- スマートフォンや狭い画面でも崩れにくい表示にする


## 4. 設定機能

以下は MVP の直後に追加候補となる設定機能です。

- 作業時間を設定できる
- 短い休憩時間を設定できる
- 長い休憩時間を設定できる
- 何回ごとに長い休憩へ入るかを設定できる
- 設定内容を保存できる


## 5. 通知と体験向上機能

以下は使い勝手を高めるための追加機能です。

- セッション完了時に音で通知できる
- セッション完了時にブラウザ通知を出せる
- 作業完了後に休憩モードへ切り替えられる
- 休憩完了後に次の作業モードへ切り替えられる
- 現在の操作が開始か再開かを明確に表示できる
- アクセシビリティを考慮したラベルや操作性を備える


## 6. 永続化とデータ管理機能

### 6.1 初期段階

- localStorage にタイマー状態を保存できる
- localStorage に当日実績を保存できる
- localStorage に設定値を保存できる


### 6.2 将来拡張

- Flask API 経由で設定を保存できる
- Flask API 経由でセッション実績を保存できる
- SQLite などの DB に履歴を保存できる


## 7. 将来拡張として考えられる機能

- 過去の日別実績を確認できる履歴画面
- 週間、月間の集中傾向を見られる分析画面
- タスク名や作業内容を紐づけたポモドーロ記録
- ユーザーごとのデータ管理
- ログイン機能や複数端末同期


## 8. テスト観点から重要な機能分解

以下の機能はユニットテストしやすいように、独立した責務として実装することを前提とします。

### 8.1 タイマーコアロジック

- タイマー開始
- 一時停止
- 再開
- リセット
- 進捗率計算
- 完了判定
- 表示用時刻フォーマット


### 8.2 統計処理

- 完了回数の加算
- 集中時間の加算
- 日付変更時のリセット判定


### 8.3 副作用を持つ処理

- 現在時刻取得
- ストレージ保存
- ストレージ読込
- DOM 更新


## 9. 実装優先順位

実装は以下の順番で進めることを推奨します。

1. Flask で画面表示できるようにする
2. モックに沿った UI を静的に再現する
3. タイマーの開始、一時停止、再開、リセットを実装する
4. 残り時間表示と円形プログレスを動的に更新する
5. セッション完了と当日実績集計を実装する
6. localStorage による保存と復元を実装する
7. 設定変更機能を実装する
8. 通知や履歴などの拡張機能を追加する


## 10. まとめ

このアプリで最初に成立させるべき中核機能は以下の 4 点です。

- タイマー制御
- 進捗表示
- 当日実績表示
- 保存と復元

これらを満たした上で、設定変更、通知、履歴表示、サーバー永続化を段階的に追加していく方針とします。