# ポモドーロタイマーアプリ 実装計画

## 1. 目的

このドキュメントは、ポモドーロタイマー Web アプリケーションを段階的に実装するための計画書です。

以下の資料と、ここまでの設計検討内容を前提にしています。

- [architecture.md](architecture.md)
- [features.md](features.md)

実装は、一度に大きく作り込むのではなく、各フェーズごとに動作確認できる単位で進めます。


## 2. 実装方針

### 2.1 基本方針

- Flask は薄いサーバーとして使う
- タイマーの実行ロジックはクライアントサイド JavaScript に持たせる
- pure function で書けるロジックを先に分離する
- localStorage による保存を先に採用し、必要に応じて API / DB を追加する
- 各フェーズでブラウザ確認可能な完成状態を作る


### 2.2 1タスクの粒度

1 つの実装タスクは、半日から 1 日程度で完了できる大きさを目安にします。

大きすぎる例:

- タイマー機能を全部実装する
- 設定機能を全部実装する

適切な例:

- Flask でトップページを返す
- モックに沿った静的 UI を作る
- 開始ボタンでカウントダウンを始める
- 一時停止と再開を追加する
- localStorage に状態を保存する


### 2.3 各フェーズの完了条件

各フェーズには以下を必ず持たせます。

- 実装対象
- 成果物
- 動作確認ポイント
- 次フェーズへ進める完了条件


## 3. フェーズ別実装計画

### フェーズ 0: プロジェクト土台の整理

目的:
Node / Express サンプル構成から、Flask ベースの構成へ切り替える。

実装対象:

- [app.py](app.py) を Flask のエントリポイントとして使えるようにする
- templates ディレクトリを追加する
- static ディレクトリを追加する
- Flask でトップページを返せる最小構成を作る
- 既存の Express 前提ファイルを今後使わない前提で整理する

成果物:

- [app.py](app.py)
- templates/index.html
- static/css/main.css
- static/js/app.js

動作確認ポイント:

- Flask でアプリが起動する
- GET / で画面が返る
- テンプレートと静的ファイルが正しく読み込まれる

完了条件:

- ブラウザでトップページが表示される
- Flask を起点とした開発に移れる


### フェーズ 1: UI モックの静的再現

目的:
添付モックに沿った見た目を HTML / CSS で再現する。

実装対象:

- ヘッダー領域の作成
- モード表示の配置
- 円形タイマー表示の見た目作成
- 時間表示テキストの配置
- 開始 / リセットボタンの配置
- 今日の進捗カードの作成
- モバイルで崩れにくいレイアウト調整

成果物:

- templates/index.html
- static/css/main.css

動作確認ポイント:

- モックに近い見た目で表示される
- PC 表示でバランスが崩れていない
- 狭い画面幅でも大きく崩れない

完了条件:

- 25:00 の静的 UI が見た目として完成している
- JavaScript のロジック実装に進める


### フェーズ 2: タイマー最小機能の実装

目的:
作業モードのタイマーを実際に動かせるようにする。

実装対象:

- 初期 state の定義
- 作業モードの残り時間管理
- 開始機能
- 一時停止機能
- 再開機能
- リセット機能
- 残り時間の画面更新

対象はまず focus モードのみに絞る。

成果物:

- static/js/domain/timer.js
- static/js/app.js
- static/js/ui/render.js

動作確認ポイント:

- 開始で 25:00 からカウントダウンが始まる
- 一時停止で時間の減少が止まる
- 再開で停止位置から再開する
- リセットで初期状態に戻る

完了条件:

- 作業タイマーとして最低限使える
- 基本状態遷移が成立している


### フェーズ 3: 進捗リングと UI 状態同期

目的:
タイマーの状態変化を UI に正しく反映する。

実装対象:

- 円形プログレスリングの進捗更新
- 現在モード表示の反映
- 開始 / 一時停止 / 再開ボタン状態の切り替え
- 実行中、停止中、初期状態の UI 表示差分の実装

成果物:

- static/js/ui/render.js
- static/js/app.js

動作確認ポイント:

- タイマー残量とリング進捗が一致する
- ボタン表示とタイマー状態が一致する
- 初期状態と実行中で UI が明確に変わる

完了条件:

- 見た目と内部状態の不一致がない
- タイマー体験として自然に操作できる


### フェーズ 4: セッション完了と当日実績の実装

目的:
タイマー完了時の処理と、当日進捗の集計を実装する。

実装対象:

- セッション完了判定
- 完了時の state 更新
- 完了回数の加算
- 集中時間の加算
- 進捗カードへの反映

成果物:

- static/js/domain/timer.js
- static/js/domain/stats.js
- static/js/ui/render.js

動作確認ポイント:

- タイマーが 0 になったときに完了処理が走る
- 今日の完了回数が増える
- 今日の集中時間が加算される
- 進捗カード表示が更新される

完了条件:

- タイマー完了が統計と連動している
- MVP の主要価値が見える状態になる


### フェーズ 5: 保存と復元の実装

目的:
リロードに耐えられるアプリにする。

実装対象:

- localStorage への state 保存
- localStorage への統計保存
- 起動時の復元処理
- 保存データ不正時のフォールバック
- 日付変更時の当日統計リセット

成果物:

- static/js/services/storage.js
- static/js/domain/stats.js
- static/js/app.js

動作確認ポイント:

- リロード後もタイマー状態が適切に復元される
- リロード後も今日の進捗が保持される
- 日付切り替えで前日データが混ざらない

完了条件:

- 日常利用に耐える安定性が出る
- MVP として十分使える状態になる


### フェーズ 6: 休憩モードの追加

目的:
ポモドーロアプリとして基本的なモード切り替えを実装する。

実装対象:

- short_break モード追加
- long_break モード追加
- モードごとの時間設定適用
- 作業回数に応じた長休憩判定
- モード表示の切り替え

成果物:

- static/js/domain/timer.js
- static/js/ui/render.js
- static/js/app.js

動作確認ポイント:

- 作業モードと休憩モードが切り替わる
- 各モードで正しい時間が表示される
- 長休憩条件が正しく反映される

完了条件:

- 基本的なポモドーロサイクルが成立する


### フェーズ 7: 設定機能の追加

目的:
固定値ではなくユーザー設定で動作するアプリにする。

実装対象:

- 作業時間設定
- 短い休憩時間設定
- 長い休憩時間設定
- 長休憩に入る周期設定
- 設定値保存

成果物:

- 設定 UI
- static/js/services/storage.js
- static/js/domain/timer.js

動作確認ポイント:

- 設定変更が保存される
- 次回表示時も設定が維持される
- 設定値でタイマーが動く

完了条件:

- ユーザーが利用スタイルに合わせて調整できる


### フェーズ 8: 通知と使い勝手の向上

目的:
毎日使いやすい体験に仕上げる。

実装対象:

- 完了音通知
- ブラウザ通知
- 自動モード遷移
- 開始 / 再開ラベルの明確化
- アクセシビリティ改善
- UI 細部の磨き込み

成果物:

- 通知関連処理
- UI 改善

動作確認ポイント:

- 完了時に通知が出る
- 自動遷移が期待通り動く
- 日常操作で迷いが少ない

完了条件:

- MVP から実用アプリへ品質を引き上げられている


### フェーズ 9: サーバー永続化と履歴拡張

目的:
将来拡張としてデータのサーバー保存と履歴表示に対応する。

実装対象:

- 設定取得 API
- 設定保存 API
- セッション完了保存 API
- SQLite などによる永続化
- 履歴画面または履歴 API

成果物:

- [app.py](app.py)
- 永続化層
- 履歴関連 UI または API

動作確認ポイント:

- 設定と実績がサーバー側に保存される
- 複数日データを参照できる

完了条件:

- localStorage 依存から段階的に拡張できる基盤が整う


## 4. フェーズ横断で意識すること

### 4.1 テストしやすい責務分離

以下は早い段階から守る。

- タイマーコアロジックは pure function 中心にする
- Date.now を直接使わず clock サービスを挟む
- localStorage は storage サービス経由にする
- DOM 更新は render 層に閉じ込める
- 初期化とイベント登録は app.js に集約する


### 4.2 実装中の確認単位

各フェーズで以下を確認する。

- 画面が壊れていないか
- 状態と UI が一致しているか
- リロードで不整合が起きないか
- 狭い画面幅でも破綻しないか
- 変更対象の責務が広がりすぎていないか


### 4.3 推奨テスト対象

優先してテストする対象:

- タイマー開始
- 一時停止と再開
- リセット
- 進捗率計算
- 完了判定
- 集中時間加算
- 日付変更時の統計リセット


## 5. 最初の 3 スプリント案

短い反復で進める場合、以下の 3 スプリントが現実的です。

### スプリント 1

- フェーズ 0 を完了する
- フェーズ 1 を完了する

ゴール:
Flask ベースでモックに近い静的 UI が見える状態にする。


### スプリント 2

- フェーズ 2 を完了する
- フェーズ 3 を完了する

ゴール:
開始、一時停止、再開、リセット、進捗リングが動作する状態にする。


### スプリント 3

- フェーズ 4 を完了する
- フェーズ 5 を完了する

ゴール:
完了判定、当日実績、保存、復元まで含めた MVP を完成させる。


## 6. MVP 完了の定義

以下を満たした時点を MVP 完了とする。

- Flask でアプリ画面が表示できる
- モックに近い UI が再現されている
- 作業タイマーの開始、一時停止、再開、リセットができる
- 残り時間とリング進捗が同期している
- セッション完了で当日実績が更新される
- localStorage による保存と復元ができる


## 7. まとめ

この実装計画では、以下の順で価値を積み上げます。

1. Flask の土台を作る
2. モックの見た目を再現する
3. タイマーの最小ロジックを実装する
4. 進捗と統計を追加する
5. 保存と復元で日常利用に耐えさせる
6. 休憩モード、設定、通知、履歴へ拡張する

この順序で進めることで、実装の複雑さを抑えながら、各段階で動作確認できる形で開発を進められます。